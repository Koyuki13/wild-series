{% extends "base.html.twig" %}

{% block body %}

    <div class="jumbotron">
        <h1 class="text-center">Wild Série</h1>
    </div>
    <div class="container">

        <div class="row row-cols-1 row-cols-md-4">
            {% for program in programs  %}
                <a href="{{ path('wild_show', { 'slug': program.title|replace({ ' ': '-' }) | lower })  }}">
                    <div class="col mb-4">
                        <div class="card h-100">
                            <img src="{{ program.poster }}" class="card-img-top" alt="{{ program.title }}">
                            <div class="card-body text-center">
                                <h6 class="card-title">{{ program.title }}</h6>
                                <small class="card-text">{{ program.category.name }}</small>
                            </div>
                        </div>
                    </div>
                </a>
            {% endfor %}
        </div>
    </div>

    {# templates/home.html.twig #}
    <a href="{{ path('wild_show', { 'slug': "walking-dead" }) }}">
        Testing show() method from WildController with a real program s slug.
    </a>
    <br>
    <a href="{{ path('wild_index') }}">
        Testing index() method from WildController to view all programs.
    </a>
{% endblock %}

