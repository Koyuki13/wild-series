{% extends "base.html.twig" %}
{% block title %}{{ slug }}{% endblock %}

{% block body %}
    <div class="container my-5">

        <div class="row">
            <div class="col-md-4">
                <img class="align-self-start mr-3" src="{{ program.poster }}" alt="{{ program.title }} poster">
            </div>
            <div class="col-md-8">
                <h1 class="mt-0 mb-5">{{ program.title }}</h1>
                <p class="h5 mb-3">{{ program.summary }}</p>
                <p class="mt-3">Categorie : {{ program.category.name }}</p>
            </div>
        </div>
        <a class="btn btn-dark my-4" href="{{ path('app_index') }}">
            Retour à l'accueil
        </a>
       {# <a class="btn btn-dark my-4" href="{{ path('wild_episode', { 'slug': episode.title|replace({ ' ': '-' }) | lower })  }}">
            Episode
        </a> #}
    </div>

{% endblock %}

