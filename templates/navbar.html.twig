<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">Wild Série</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav ml-auto">
            <a class="nav-item nav-link mx-2" href="{{ path('app_index') }}">Accueil</a>
            <a class="nav-item nav-link mx-2" href="{{ path('wild_index') }}">Wild</a>
            <a class="nav-item nav-link mx-2" href="/program">Liste des séries</a>
        {% if app.user %}
            {{ app.user.email }}
            <a class="nav-item nav-link mx-2" href="{{ path('app_logout') }}"> Se déconnecter</a>
        {% else %}
            <a class="nav-item nav-link mx-2" href="{{ path('app_login') }}"> Se connecter</a>
        {% endif %}
        </div>
    </div>
</nav>